name: Build Angular Editor

on:
  workflow_dispatch:
  push:
    paths:
      - 'src/SharpOMatic.Editor/**'
  pull_request:
    paths:
      - 'src/SharpOMatic.Editor/**'

jobs:
  build-angular:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: 'src/SharpOMatic.Editor'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: 'src/SharpOMatic.Editor/package-lock.json'          

      - name: Install dependencies
        run: npm ci              

      - name: Build Angular app
        run: npm run build
